import type { CSSProperties } from "react";

import { surfaceGhost } from "./styles/tokens";

export const notFoundCard: CSSProperties = {
  background: surfaceGhost,
};

export const notFoundTitle: CSSProperties = {
  letterSpacing: "-0.02em",
};
